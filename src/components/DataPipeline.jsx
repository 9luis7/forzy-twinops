// DataPipeline.jsx — as 6 etapas clicáveis do ciclo de vida do dado.
// [1] SENSOR -> [2] BROKER -> [3] INGESTÃO -> [4] SUPABASE -> [5] INTERFACE -> [6] DECISÃO
// Estrela do protótipo. STUB — a construir.

export default function DataPipeline() {
  return (
    <section style={{ background: "var(--surface)", border: "1px solid var(--borda)", borderRadius: 12, padding: 16 }}>
      <h2 style={{ marginTop: 0 }}>Pipeline do dado</h2>
      <p style={{ color: "var(--texto-fraco)" }}>STUB: sensor → broker → ingestão → supabase → interface → decisão (etapas clicáveis).</p>
    </section>
  );
}
