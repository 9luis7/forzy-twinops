// AssetPanel.jsx — ficha técnica + leitura atual (temp, corrente, vibração, rotação). STUB — a construir.

export default function AssetPanel() {
  return (
    <section style={{ background: "var(--surface)", border: "1px solid var(--borda)", borderRadius: 12, padding: 16 }}>
      <h3 style={{ marginTop: 0 }}>Painel do ativo</h3>
      <p style={{ color: "var(--texto-fraco)", fontSize: 13 }}>STUB: cards de leitura atual + ficha (TAG, tipo, setor) + estado operacional.</p>
    </section>
  );
}
