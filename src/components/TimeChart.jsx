// TimeChart.jsx — série temporal das leituras (destaque da tendência no MOT-003). STUB — a construir.

export default function TimeChart() {
  return (
    <section style={{ background: "var(--surface)", border: "1px solid var(--borda)", borderRadius: 12, padding: 16, marginTop: 16 }}>
      <h3 style={{ marginTop: 0 }}>Gráfico temporal</h3>
      <p style={{ color: "var(--texto-fraco)", fontSize: 13 }}>STUB: série histórica (recharts) com tendência de degradação visível.</p>
    </section>
  );
}
