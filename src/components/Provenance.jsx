// Provenance.jsx — trilha de auditoria por leitura (sensor, quando, condição, doc de apoio, validação humana). STUB — a construir.

export default function Provenance() {
  return (
    <section style={{ background: "var(--surface)", border: "1px solid var(--borda)", borderRadius: 12, padding: 16, marginTop: 16 }}>
      <h3 style={{ marginTop: 0 }}>Procedência / auditoria</h3>
      <p style={{ color: "var(--texto-fraco)", fontSize: 13 }}>STUB: origem rastreável de cada leitura + validação humana.</p>
    </section>
  );
}
