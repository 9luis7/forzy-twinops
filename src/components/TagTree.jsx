// TagTree.jsx — navegação Planta -> Setor -> Motor por TAG. STUB — a construir.

export default function TagTree() {
  return (
    <aside style={{ background: "var(--surface)", border: "1px solid var(--borda)", borderRadius: 12, padding: 16 }}>
      <h3 style={{ marginTop: 0 }}>Árvore TAG</h3>
      <p style={{ color: "var(--texto-fraco)", fontSize: 13 }}>STUB: Planta → Setor → Motor.</p>
    </aside>
  );
}
